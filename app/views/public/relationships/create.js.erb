$(".follow_list_<%= @user.id %>").html("<%= j(render partial: "public/relationships/follow", locals: {user: @user} ) %>");
<!--タブ間での非同期通信（タブ切り替えの際に更新されるようにするため）-->
$('#follow').html("<%= j(render 'public/relationships/follow_list', users: @users1) %>");